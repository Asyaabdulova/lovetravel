1) реализовать функцию concatArray, которая на вход принимает 2 аргумента:
   - первый аргумент - массив ($arr);
   - второй - строковый символ ($symb);
   Ф-ция должна объеденить все элементы массива в одну строку и вернуть ее.
   (элементы в строке должны быть разделены между собой значением переменной $symb)
   
2) реализовать функцию xPow, возведения числа в степень
   ф-ция должна принимать 2 параметра:
    - число, которое необходимо возвести в степень ($a)
	- степень числа ($n)
	aф-ция должна вернуть в качестве результата $a в степени $n
	
3) реализовать функцию sqr, которая вернет квадрат числа (число в степени 2)
   ф-ция должна принимать 1 параметр:
    - число, квадрат которого необходимо вернуть ($a)
	ф-ция должна вернуть в качестве результата $a в степени 2
	
4) реализовать функцию showText, которая выведет на экран любую фразу
   ф-ция должна принимать 1 параметр:
    - произвольная строка ($str)
   ф-ция должна вывести на экран содержимое переменной $str
   
5) реализовать функцию explodeArray, на вход 2 аргумента:
    - строка ($str)
    - строковый символ ($symb)
   ф-ция должна вернуть МАССИВ который получиться при разбиении строки по символу $symb.
   Например: $str = "asd,frty,hhhre";
             $symb = ",";
			 результатом выполнения ф-ции должен получиться массив ["asd","frty","hhhre"]
			 
6) реализовать функцию showArray, которая выведет на экран массив
   ф-ция должна принимать 1 параметр:
    - массив ($arr)
   результат выполнения ф-ции - вывод массива $arr на экран
   
7) реализовать функцию calc, которая принимает на вход 3 параметра:
    - первый, число ($a)
    - второй, число ($b)
    - третий, знак операции ($sign)
   Если третим параметром ($sign) передано:
    "+" - сложить $a и $b;
    "-" - вычесть из $a $b;
    "*" - умножить $a на $b;
    "/" - разделить $a на $b;
   результат вычеслительной операции вернуть из ф-ции.
   В случае если 3 аргументом передано не "+,-,*,/", то вернуть false.
   
8) реализовать функцию hello, которая принимает на вход 1 параметр:
	- строка, имя ($name)
   ф-ция должна вывести на экран фразу "Приветствую тебя, ХХХХ!",
   где вместо ХХХХ вы должны подставить значение переменной $name
   
9) реализовать функцию getUser, которая НЕ принимает на вход параметры.
   Ф-ция должна вернуть асоциативный массив, который должен содержать следующие пункты (асоциации):
    - name, имя пользователя
    - email, электронный ящик пользователя
    - birhtday, день рождения пользователя
   Данные в массиве заполнить произвольными значениями.
   
10) реализовать функцию deleteFromArray, которая принимает на вход 2 аргумента:
     - первый, массив ($arr)
     - второй, элемент в массиве ($el)
	ф-ция должна вернуть массив из которого удалено значение совпадающее с $el.
	Например: $arr = [1,45,"ddd","ase",true,19];
	          $el = "ase";
			  результат выполнения ф-ции должен быть массив [1,45,"ddd",true,19]
